<p id="notice"><%= notice %></p>
<div class="container">
  <div class="jumbotron">
    <h1><%= @project.title %></h1>
      <p>
        <%= @project.description %>
        ...
      </p>
    <span class="label label-primary"> Last update <%= @project.updated_at.strftime("%b %d, %Y") %></span>
  </div>
</div>
<div class="container">
  <div class="panel panel-default">
    <div class="panel-body">
      
    </div>
    <%= link_to 'Back', root_path, class: 'label label-primary' %>
  </div>
</div>
